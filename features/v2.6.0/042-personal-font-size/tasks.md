# Tasks: 个人字体大小

**关联规格**: [spec.md](./spec.md)
**版本**: v2.6.0

## 状态

| 步骤 | 状态 | 备注 |
|---|---|---|
| spec.md | ✅ 已评审 | 依据用户提供的完整规格 |
| design.md | ✅ 已评审 | 用户已授权直接实现并部署独立环境 |
| tasks.md | ✅ 已拆解 | 6 个纵向任务 |
| 实现 | 🔄 修复中 | 账号偏好、双入口与语义 token 已发布到 13100；管理后台内联展开需改为左侧独立气泡 |

## Tasks

- [x] **T001**: 用户偏好模型、Repository、Service 与单元测试
  **覆盖 AC**: AC-04, AC-06
- [x] **T002**: `/user/info` 字段与字体大小更新接口
  **覆盖 AC**: AC-04, AC-05, AC-06
- [x] **T003**: 两个 SPA 的语义字体、图标、容器和布局 token
  **覆盖 AC**: AC-03, AC-07, AC-08
- [x] **T004**: 工作台与管理后台个人菜单的标品 7 档与中粮 3 档控件
  **覆盖 AC**: AC-01, AC-02, AC-05, AC-09
- [x] **T005**: 构建、提交与隔离环境部署；浏览器效果由用户手工验收
  **覆盖 AC**: AC-01–AC-09
- [ ] **T006**: 管理后台字号控件改为左侧独立二级气泡，更新 PRD 原型并重新部署
  **覆盖 AC**: AC-01, AC-09, AC-10

## 历史验收记录

- client 与 platform 的本地、服务器生产构建均通过。
- 后端模型发现、建表、默认值、更新与越界校验通过；未登录更新接口返回 401。
- 标品原型验证 7 个刻度、档位 3 → 7、刷新恢复、语义文字/图标/侧栏 token 联动，页面无横向溢出。
- 1024px、第 7 档下二级侧栏默认折叠，用户可手动重新展开。
- 生产隔离环境健康；共享环境保持可用且未修改。

## 实际偏差记录

- 仓库全量 `tsc --noEmit` 仍包含既有基线错误；本功能涉及文件未新增类型错误，Vite 生产构建通过。
- 隔离环境暂无可用于验收的已登录应用账号，因此真实部署仅完成登录页、静态资源、后端模型/API 与容器健康验证；登录后的视觉交互由同一前端代码的 mock 原型完成。

## 本轮补充

- 2026-07-24 用户确认管理后台也需要在个人菜单提供【字体大小】，两端复用交互并读写同一份账号配置。
- 2026-07-27 用户明确要求跳过 Codex 浏览器首轮与回归，直接合并、重启前后端服务，并由用户在部署后手工确认 UI 效果；自动 UI 用例维持 `NOT_RUN`，不据此声明验收通过。
- 2026-07-27 用户根据 13100 截图确认管理后台当前内联展开方式层级混乱，要求与工作台保持同一二级气泡逻辑，但根据页面位置改为向个人主菜单左侧弹出。
