# 工作台知识空间下拉分类验收报告

**环境**：`http://192.168.106.114:13100/`  
**用例**：[acceptance-test-cases.md](./acceptance-test-cases.md)  

## 第一轮：变更前现状

第一轮已在改代码前完成。13100 当前运行提交为 `55ab8aa7139f541752365052225e62ea5f68b3e4`。浏览器可正常打开登录页，但当前浏览器没有登录态；根据环境规则，本轮不自动创建账号、不改密码、不读取或写入业务凭据，因此登录后的 UI 用例记为 `BLOCKED`。源码检查确认当前实现把三类空间合并为一个无分组列表，这只是代码差异证据，不替代运行时 UI 验收。

| Case ID | 状态 | 实际结果 | 差异 / Bug | 证据 |
|---|---|---|---|---|
| TC-001 | BLOCKED | 浏览器停留在登录页，无法进入工作台 | 缺少已授权测试登录态 | 13100 登录页；源码 `ChatKnowledge.tsx` 当前合并三类空间 |
| TC-002 | BLOCKED | 同上 | 同上 | 同上 |
| TC-003 | BLOCKED | 同上 | 同上 | 同上 |
| TC-004 | BLOCKED | 同上 | 同上 | 同上 |
| TC-005 | BLOCKED | 同上 | 同上 | 同上 |
| TC-006 | PASS | `13100`、`/api/v1/env`、`13000`、`3001` 均返回 HTTP 200；独立环境核心容器正常运行 | — | 服务器只读检查 |

## 第二轮：部署后完整回归

候选提交 `4b771c38629f2f0101ee28b9444860f29edb3a46` 已于 2026-07-27 部署到独立 `13100`。本地和服务器 client 生产构建、服务器 platform 生产构建均成功；服务器源码 SHA-256 与本地候选文件一致，client 产物包含本次分组标记。浏览器重新加载后登录页和静态资源正常，但仍无已授权登录态，因此登录后的 UI 用例保持 `BLOCKED`。

| Case ID | 状态 | 实际结果 | 差异 / Bug | 证据 |
|---|---|---|---|---|
| TC-001 | BLOCKED | 已部署分组代码；缺少登录态，未在真实工作台操作下拉框 | 缺少已授权测试登录态 | 定向单测；服务器源码与构建标记 |
| TC-002 | BLOCKED | 分组过滤单测通过；未执行登录后搜索 UI | 同上 | `knowledgeSpaceGrouping.test.ts` |
| TC-003 | BLOCKED | 选择处理函数未改；未执行登录后选择 UI | 同上 | 代码差异检查 |
| TC-004 | BLOCKED | 本次未修改 locale、搜索 placeholder 或输入框组件；未执行登录后视觉比对 | 同上 | 代码差异检查 |
| TC-005 | BLOCKED | 桌面端与移动端复用同一分组数据；未执行登录后移动端 UI | 同上 | 生产构建；代码差异检查 |
| TC-006 | PASS | `13100` 根入口和 `/api/v1/env`、OpenFGA、`13000`、`3001` 均返回 HTTP 200；独立环境容器正常运行 | — | 部署后服务器只读检查 |

## 自动化与部署检查

- `knowledgeSpaceGrouping.test.ts`：3/3 PASS，覆盖固定顺序、部门空间去重、默认空栏目保留、搜索空分类隐藏。
- `npm run check-imports`：PASS。
- client 本地生产构建：PASS。
- client / platform 服务器生产构建：PASS；只有项目既有的包体积、浏览器数据和 PWA 静态图标匹配警告。
- 服务器部署标记：`4b771c38629f2f0101ee28b9444860f29edb3a46`。
- 服务器备份：`/opt/codex-bisheng/.codex-deploy-backups/knowledge-space-groups-20260727-predeploy`。

## 结论

候选代码和构建产物已经在 `13100` 运行，环境与部署边界检查通过。由于没有登录态，TC-001–TC-005 不能标记为 UI 验收通过；需由已登录用户在同一环境完成最终页面确认。
